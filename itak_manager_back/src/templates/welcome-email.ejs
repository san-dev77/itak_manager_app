<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bienvenue sur <%= appName %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      line-height: 1.6;
      color: #222222; /* foreground */
      background-color: #ffffff; /* background */
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff; /* card */
      border-radius: 12px;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .header {
      background: linear-gradient(135deg, #4a6cf7 0%, #3755c7 100%);
      padding: 40px 30px;
      text-align: center;
      color: #ffffff !important; /* primary-foreground */
    }

    .logo {
      width: 100px;
      height: 100px;
      margin: 0 auto 20px;
      background-color: rgba(255, 255, 255, 0.1);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .logo img {
      width: 90%;
      height: 90%;
      object-fit: contain;
    }

    .header h1 {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .header p {
      font-size: 16px;
      opacity: 0.9;
    }

    .content {
      padding: 40px 30px;
    }

    .welcome-message {
      text-align: center;
      margin-bottom: 30px;
    }

    .welcome-message h2 {
      font-size: 24px;
      font-weight: 600;
      color: #222222; /* foreground */
      margin-bottom: 12px;
    }

    .welcome-message p {
      font-size: 16px;
      color: #666666; /* muted-foreground */
    }

    .credentials-card {
      background-color: #f7f7f7; /* muted */
      border: 2px solid #e0e0e0; /* border */
      border-radius: 12px;
      padding: 24px;
      margin: 30px 0;
    }

    .credentials-title {
      font-size: 18px;
      font-weight: 600;
      color: #222222; /* foreground */
      margin-bottom: 16px;
      display: flex;
      align-items: center;
    }

    .credentials-title::before {
      content: "🔐";
      margin-right: 8px;
    }

    .credential-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 0;
      border-bottom: 1px solid #e0e0e0; /* border */
    }

    .credential-item:last-child {
      border-bottom: none;
    }

    .credential-label {
      font-weight: 500;
      color: #222222;
    }

    .credential-value {
      font-family: 'Courier New', monospace;
      background-color: #ffffff; /* background */
      padding: 6px 12px;
      border-radius: 6px;
      border: 1px solid #e0e0e0;
      font-size: 14px;
      color: #222222;
    }

    .role-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 500;
      text-transform: uppercase;
    }

    .role-super-admin {
      background-color: #fef3c7;
      color: #b45309;
    }

    .role-admin {
      background-color: #ffe4e6;
      color: #d72638;
    }

    .role-scolarite {
      background-color: #e6f0ff;
      color: #1e40af;
    }

    .role-finance {
      background-color: #dcfce7;
      color: #16a34a;
    }

    .role-qualite {
      background-color: #f3e8ff;
      color: #7c3aed;
    }

    .cta-button {
      display: inline-block;
      background: linear-gradient(135deg, #4a6cf7 0%, #3755c7 100%);
      color: #ffffff !important; /* primary-foreground */
      text-decoration: none;
      padding: 14px 28px;
      border-radius: 8px;
      font-weight: 600;
      font-size: 16px;
      text-align: center;
      margin: 30px auto;
      display: block;
      max-width: 200px;
      transition: transform 0.2s ease;
    }

    .cta-button:hover {
      transform: translateY(-2px);
    }

    .info-section {
      background-color: #f0f4ff;
      border-left: 4px solid #4a6cf7; /* primary */
      padding: 20px;
      margin: 30px 0;
      border-radius: 0 8px 8px 0;
    }

    .info-section h3 {
      color: #1e40af;
      font-size: 16px;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .info-section ul {
      list-style: none;
      padding-left: 0;
    }

    .info-section li {
      padding: 4px 0;
      color: #1e40af;
    }

    .info-section li::before {
      content: "✓";
      color: #16a34a; /* green check */
      font-weight: bold;
      margin-right: 8px;
    }

    .footer {
      background-color: #ffffff;
      padding: 30px;
      text-align: center;
      border-top: 1px solid #e0e0e0;
    }

    .footer p {
      color: #666666;
      font-size: 14px;
      margin-bottom: 8px;
    }

    .footer a {
      color: #4a6cf7; /* primary */
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    @media (max-width: 600px) {
      .container {
        margin: 0;
        border-radius: 0;
      }

      .header, .content, .footer {
        padding: 30px 20px;
      }

      .credentials-card {
        padding: 20px;
      }

      .credential-item {
        flex-direction: column;
        align-items: flex-start;
        gap: 8px;
      }

      .credential-value {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="logo">
        <img src="<%= logoUrl %>" alt="<%= appName %> Logo">
      </div>
      <h1><%= appName %></h1>
      <p>Système de Gestion Universitaire</p>
    </div>

    <!-- Content -->
    <div class="content">
      <div class="welcome-message">
        <h2>Bienvenue <%= firstName %> <%= lastName %> !</h2>
        <p>Votre compte a été créé avec succès. Vous pouvez maintenant accéder à votre espace de gestion.</p>
      </div>

      <!-- Credentials Card -->
      <div class="credentials-card">
        <div class="credentials-title">Vos informations de connexion</div>
        <div class="credential-item">
          <span class="credential-label">Email :</span>
          <span class="credential-value"><%= email %></span>
        </div>
        <div class="credential-item">
          <span class="credential-label">Mot de passe :</span>
          <span class="credential-value"><%= password %></span>
        </div>
        <div class="credential-item">
          <span class="credential-label">Rôle :</span>
          <span class="role-badge role-<%= role.toLowerCase().replace('_', '-') %>">
            <%= role === 'super_admin' ? 'Président / DG' : 
                role === 'admin' ? 'Administrateur' : 
                role === 'scolarite' ? 'Service Scolarité' :
                role === 'finance' ? 'Service Comptabilité' :
                role === 'qualite' ? 'Assurance Qualité' : role %>
          </span>
        </div>
      </div>

      <!-- CTA -->
      <a href="<%= loginUrl %>" class="cta-button">Se connecter</a>

      <!-- Info Section -->
      <div class="info-section">
        <h3>Prochaines étapes :</h3>
        <ul>
          <li>Connectez-vous avec vos identifiants</li>
          <li>Changez votre mot de passe lors de votre première connexion</li>
          <li>Accédez à votre espace de travail</li>
          <li>Contactez le support en cas de problème</li>
        </ul>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>
        Besoin d'aide ? Contactez notre support : 
        <a href="mailto:<%= supportEmail %>"><%= supportEmail %></a>
      </p>
      <p>© <%= currentYear %> <%= appName %>. Tous droits réservés.</p>
    </div>
  </div>
</body>
</html>
